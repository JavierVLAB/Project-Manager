version: '3.8'

services:
  web:
    build: .
    container_name: nextjs-app
    ports:
      - "53210:53210"
    restart: always
    volumes:
      # Carpeta persistente en el servidor (no se borra al reconstruir)
      - ./public/local_permanent:/app/public/local_permanent
